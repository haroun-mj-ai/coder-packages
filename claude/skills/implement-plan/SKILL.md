---
name: implement-plan
description: RETIRED — superseded by implement-issue's --phase implement. Kept only so old invocations fail informatively; do not extend this file.
---

# implement-plan (retired)

Folded into `implement-issue` (`--phase implement <plan-path>`), which
produces the same committed branch, durable QA artifact, and headless
contract this skill used to. See `../implement-issue/SKILL.md`.
