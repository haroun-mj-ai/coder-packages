---
name: plan-issue
description: RETIRED — superseded by implement-issue's --phase plan. Kept only so old invocations fail informatively; do not extend this file.
---

# plan-issue (retired)

Folded into `implement-issue` (`--phase plan`), which produces the same
committed plan file, worktree convention, and headless contract this skill
used to. See `../implement-issue/SKILL.md`.

Templates formerly here (`plan-template.md`, `plan-template-light.md`) moved
to `../implement-issue/`.
